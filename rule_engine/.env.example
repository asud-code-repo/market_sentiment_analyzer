# Copy to .env for local testing (gitignored). In GitHub Actions these come
# from repo secrets, injected as env vars in the workflow.
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
