{
  "_comment": "Merge the 'crash-check' entry below into your actual Claude Desktop config (not this file, which is just a template). On Windows that's %APPDATA%\\Claude\\claude_desktop_config.json. Requires Node.js installed locally — this runs as a subprocess via npx/tsx, not inside this repo's CI.",
  "mcpServers": {
    "crash-check": {
      "command": "npx",
      "args": ["tsx", "C:\\ABSOLUTE\\PATH\\TO\\market_sentiment_analyzer\\mcp_server\\src\\server.ts"],
      "env": {
        "SUPABASE_URL": "https://YOUR_PROJECT_REF.supabase.co",
        "SUPABASE_SERVICE_ROLE_KEY": "YOUR_SECRET_KEY",
        "PORTFOLIO_PATH": "C:\\ABSOLUTE\\PATH\\TO\\market_sentiment_analyzer\\local_state\\portfolio.yaml"
      }
    }
  }
}
