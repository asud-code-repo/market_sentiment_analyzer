{
  "name": "crash-check-mcp-server",
  "version": "1.0.0",
  "private": true,
  "description": "Stage 4 — local MCP server (stdio) for Claude Desktop. Merges Supabase macro/rule state with local_state/portfolio.yaml personal data at read-time only; never writes portfolio data anywhere.",
  "type": "module",
  "scripts": {
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@supabase/supabase-js": "^2.45.4",
    "js-yaml": "^5.2.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
